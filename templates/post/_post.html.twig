{% for post in posts %}
    <h2>{{ post.title }}</h2>
    <small>Dans la catégorie :
        {{ post.category.title }}

    </small>
    <br>
    <img src="{{post.image}}" alt="{{post.title}}">
    <p>{{ post.introduction }}</p>
    <ul>
        {% for tag in post.tags %}
            <li>{{tag.title}}</li>
        {% endfor %}
    </ul>
{% endfor %}
